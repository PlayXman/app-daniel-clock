{
  "name": "nuxt-app",
  "private": true,
  "engines": {
    "node": ">=16"
  },
  "scripts": {
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "postinstall": "nuxt prepare",
    "deploy": "firebase deploy"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "@vite-pwa/nuxt": "^0.1.0",
    "bignumber.js": "^9.1.2",
    "nuxt": "^3.7.0"
  }
}
